# شرح كود مشروع مداد (Midad)

هذا الملف يشرح كل جزء من كود المشروع بالتفصيل: وش وظيفته، ووين تلقاه، وكيف يشتغل مع باقي الأجزاء. الهدف إنك تقدرين ترجعين له أي وقت تبين تعدلين شي أو تفهمين ليش سوّيته كذا.

---

## ١. الفكرة العامة للمشروع

مداد منصة تعليمية تفاعلية تحوّل الدروس الدراسية إلى "مغامرة قصصية" تدور في مناطق من المملكة (الأحساء، الربع الخالي...). الطالب يختار شخصية، يدخل مغامرة فيها قصة + قرارات + أسئلة، وفي النهاية يشوف نتيجته وتقييمه.

**المسار اللي يمشي فيه المستخدم (وهو نفسه مسار الشاشات في الكود):**

```
الصفحة الرئيسية (Landing)
      ↓
لوحة التحكم (Dashboard) — اختيار المادة/الدرس
      ↓
إعداد المغامرة (Setup) — اختيار تفاصيل الدرس
      ↓
اختيار الشخصية (Character)
      ↓
اختيار المنطقة على الخريطة (Map)
      ↓
شاشة التحضير (Generating)
      ↓
المغامرة نفسها (Adventure) — القصة والأسئلة
      ↓
النتائج (Results)
```

كل شاشة من هذي = ملف/مكوّن React مستقل، و`App.tsx` هو المايسترو اللي يقرر أي شاشة تظهر بالضبط.

---

## ٢. البنية التقنية (Tech Stack)

المشروع مبني بـ:

- **React 19** — مكتبة بناء الواجهات.
- **TypeScript** — جافاسكربت مع أنواع بيانات ثابتة (يمنع أخطاء كثيرة قبل حتى ما تشغلين الكود).
- **Vite 8** — أداة التشغيل والبناء (هي اللي تسوي `npm run dev` وتطلع لك الموقع محلياً بسرعة).
- **Tailwind CSS v4** — تنسيق عن طريق كلاسات جاهزة (`flex`, `rounded-xl`, ...) بدل ما تكتبين CSS يدوي لكل شي.

ما فيه أي backend أو قاعدة بيانات حالياً — المشروع كله يشتغل على المتصفح (client-side)، والحفظ الوحيد للبيانات هو عن طريق `localStorage` بالمتصفح (بنشرحه في قسم `progress.ts`).

---

## ٣. هيكل الملفات

```
مداد/
├── index.html                 نقطة الدخول HTML
├── vite.config.ts             إعدادات Vite
├── package.json               معلومات المشروع والمكتبات
├── src/
│   ├── main.tsx                نقطة دخول React (تشغّل App)
│   ├── App.tsx                 المايسترو - يدير كل الشاشات
│   ├── index.css               كل التنسيق والألوان والخطوط
│   ├── components/              كل شاشة = ملف
│   │   ├── LandingScreen.tsx
│   │   ├── SetupScreen.tsx
│   │   ├── CharacterScreen.tsx
│   │   ├── MapScreen.tsx
│   │   ├── GeneratingScreen.tsx
│   │   ├── AdventureScreen.tsx
│   │   ├── ResultsScreen.tsx
│   │   ├── Stars.tsx            خلفية نجوم متحركة (تستخدم بأكثر من شاشة)
│   │   ├── MatchPuzzle.tsx      لعبة "وصّل" داخل المغامرة
│   │   ├── SceneTransition.tsx  أنيميشن الانتقال بين مشاهد القصة
│   │   └── StoryMapPanel.tsx    اللوحة الجانبية اللي تعرض تقدمك بالقصة
│   └── data/                    البيانات الثابتة (القصص، الشخصيات، التقدّم)
│       ├── stories.ts
│       ├── characters.ts
│       └── progress.ts
```

---

## ٤. `App.tsx` — المايسترو اللي يدير كل شي

هذا أهم ملف بالمشروع لأنه المسؤول عن "أي شاشة تظهر الحين". الفكرة تسمى **state machine** (آلة حالات) بسيطة:

```ts
type Screen = "landing" | "dashboard" | "setup" | "character"
            | "map" | "generating" | "adventure" | "results";
```

عنده متغيّر واحد اسمه `screen` يحدد الشاشة الحالية، وكل شاشة عندها زر أو حدث يغيّر قيمة `screen` وينقل المستخدم للشاشة الجاية. مثلاً لما تضغطين "ابدأ" بشاشة الخريطة، الكود يسوي:

```ts
setScreen("generating")
```

وبنفس اللحظة React يعيد رسم الصفحة ويطلع مكوّن `GeneratingScreen` بدل `MapScreen`.

بالإضافة لهذا، `App.tsx` يحمل **البيانات المشتركة** بين الشاشات (لأن كل شاشة مكوّن منفصل ومحتاجة تعرف نفس المعلومات):

| المتغيّر | وش يحفظ |
|---|---|
| `selectedPreset` | المادة/الدرس اللي اختاره الطالب من الداشبورد |
| `lessonInfo` | تفاصيل الدرس اللي عبّاها بشاشة Setup |
| `character` | الشخصية اللي اختارها الطالب |
| `gameState` | تقدم المغامرة الحالية (النقاط، المفاهيم المتقنة...) |
| `profile` | ملف الطالب الدائم (المحفوظ بـ localStorage) |

هذا النمط يشبه "تمرير الأمانة" — كل شاشة توصل بياناتها لـ `App.tsx`، وهو يمررها للشاشة اللي بعدها.

---

## ٥. الشاشات (`src/components/`)

### LandingScreen.tsx
الصفحة الرئيسية (اللي يشوفها أي زائر أول ما يفتح الموقع). فيها:
- **هيرو متحرك** مع تأثير parallax (قمر ونجوم متطايرة (fireflies) وكثبان رملية SVG تتحرك بسرعات مختلفة حسب سكرول الصفحة).
- **كاروسيل مزايا** المشروع.
- **بطاقات المواد الدراسية** (subject presets) — كل بطاقة تمثل مادة/درس يقدر الطالب يبدأ فيه.
- **قسم "كيف يعمل"** بأربع خطوات.
- **إحصائيات + دعوة لبدء الاستخدام (CTA)**.
- **فوتر**.

هذا الملف هو "النموذج الصحيح" اللي قسنا عليه إصلاح باقي الشاشات، لأن الحاوية الرئيسية فيه كانت مكتوبة صح من البداية:
```tsx
<div className="landing-scroll h-full overflow-y-auto">
```

### SetupScreen.tsx / CharacterScreen.tsx / MapScreen.tsx / GeneratingScreen.tsx
هذي الشاشات الأربع فيها نفس الغرض تقريباً: كل وحدة تاخذ قرار واحد من الطالب (تفاصيل الدرس → الشخصية → المنطقة → شاشة تحميل بسيطة) وتنقله للي بعدها. كل وحدة فيها بطاقات اختيار وزر "متابعة".

**هذي الأربع شاشات فيها كانت البق (bug) اللي صلحناه** — راجعي القسم ٨ بالأسفل لشرح المشكلة والحل بالتفصيل.

### AdventureScreen.tsx — قلب المشروع
هذا أعقد وأهم مكوّن بالمشروع، لأنه المسؤول عن عرض القصة نفسها وتفاعل الطالب معها. أهم أجزاءه:

- **`nodeId`**: المتغيّر اللي يحدد "أي عقدة (node) من القصة إحنا فيها الحين". القصة مبنية كأنها شجرة قرارات — كل عقدة لها نوع ومحتوى، ولما يختار الطالب، الكود يغيّر `nodeId` للعقدة التالية.
- **تأثير الكتابة (typewriter effect)**: النص ما يطلع دفعة وحدة، يطلع حرف حرف بسرعة معينة عشان يعطي إحساس إن أحد "يحكي" لك القصة.
- **انتقال المشاهد (Scene Transition)**: لما تتغير `scene` (المشهد، مثل الانتقال من مكان لمكان بالقصة)، يظهر مكوّن `SceneTransition` كأنيميشن انتقالي بين المشهدين.
- **أنواع العقد (node types)**: كل عقدة بالقصة تقدر تكون:
  - **narrative** — سرد قصصي عادي (بدون قرار).
  - **choice** — قرار: الطالب يختار بين خيارين أو أكثر.
  - **question** — سؤال تعليمي مباشر.
  - **puzzle / match** — لعبة توصيل (تفتح مكوّن `MatchPuzzle`).
  - **ending** — نهاية المغامرة (يوديك على شاشة النتائج).
- **نظام التلميحات (hints)**: لو الشخصية اللي اختارها الطالب هي "الشافي/المعالج" (character bonus)، يحصل على تلميحات مجانية أثناء المغامرة.
- **تتبع النقاط والمفاهيم**: كل إجابة صح تزيد `score` وتضيف مفهوم لقائمة `conceptsMastered` اللي بتنعرض بشاشة النتائج.
- **`StoryMapPanel`**: لوحة جانبية تعرض للطالب وين وصل بالقصة (خريطة تقدم مصغّرة).

هيكل الصفحة نفسه مبني صح من الأساس (ما فيه بق سكرول فيه):
```tsx
<div className="relative h-screen flex flex-col overflow-hidden">
  ...
  <div className="flex-1 overflow-y-auto relative z-10">
    {/* محتوى القصة القابل للسكرول */}
  </div>
</div>
```
لاحظي الفرق: هنا `h-screen` مضبوطة على الحاوية الخارجية فقط (وما فيها overflow يبيّن المحتوى)، والسكرول الفعلي يصير بالحاوية الداخلية `flex-1 overflow-y-auto`. هذا هو النمط الصحيح.

### ResultsScreen.tsx
شاشة النتائج بعد ما يخلص الطالب المغامرة. فيها:
- **نسبة مئوية متحركة (animated score)**: الرقم يطلع من ٠ ويطلع بالتدريج للنتيجة الحقيقية خلال ١.٢ ثانية (بستخدم `setInterval`).
- **حلقة النتيجة (SVG score ring)**: دائرة تتلوّن تدريجياً حسب النسبة، مرسومة بـ SVG باستخدام `stroke-dasharray` محسوبة من محيط الدائرة.
- **النجوم**: ١ لـ ٣ نجوم حسب النسبة (٨٥٪+ = ٣ نجوم، ٦٠-٨٤٪ = نجمتين، أقل = نجمة).
- **تقرير المفاهيم**: شريط تقدم لكل مفهوم بالدرس، يبيّن إذا الطالب أتقنه أو يحتاج مراجعة.
- **تقييم الذكاء الاصطناعي**: نص ثابت (مو مولّد فعلياً من AI حالياً) يتغيّر حسب فئة النتيجة (ممتاز / جيد / يحتاج تحسين).
- **الإنجازات (Achievements)**: ٥ أوسمة مختلفة (أول انتصار، المثالي، العالِم، بلا تلميح، ابن الوطن) تُمنح حسب شروط بسيطة (مثلاً "المثالي" يحتاج نسبة ٩٠٪+).
- **أزرار العب مرة أخرى / اختر مغامرة جديدة**.

هذي الشاشة فيها نفس بق السكرول (تفاصيله بالقسم ٨).

---

## ٦. البيانات (`src/data/`)

### `stories.ts` — أهم ملف بيانات بالمشروع
فيه التعريفات (types) اللي تبني عليها أي قصة جديدة:

```ts
interface Choice {
  text: string;        // نص الخيار اللي يشوفه الطالب
  correct?: boolean;   // هل هذا الخيار صحيح تعليمياً؟
  nextNode: string;    // العقدة اللي ينتقل لها لو اختاره
  feedback?: string;   // رسالة تفسيرية بعد الاختيار
}

interface MatchPair {
  left: string;
  right: string;
}

interface StoryNode {
  id: string;
  type: "narrative" | "choice" | "question" | "puzzle" | "match" | "ending";
  scene?: string;
  text: string;
  choices?: Choice[];
  matchPairs?: MatchPair[];
  concept?: string;    // المفهوم التعليمي المرتبط بهذي العقدة
  nextNode?: string;
}

interface Story {
  id: string;
  title: string;
  subject: string;
  region: string;
  grade: string;
  concepts: string[];
  nodes: StoryNode[];
}
```

هذا الملف فيه حالياً **قصتين كاملتين جاهزتين**:
1. **`waterCycleStory`** — دورة الماء في الأحساء (٩ عقد).
2. **`foodChainsStory`** — السلاسل الغذائية في الربع الخالي (٧ عقد).

وفيه `subjectPresets` — مصفوفة فيها ٤ بطاقات مواد تظهر بشاشة الداشبورد/الرئيسية، اثنتين منها مرتبطتين بقصة فعلية شغالة، واثنتين "قريباً" (بدون قصة حقيقية بعد).

**هذا الملف هو أهم مكان تضيفين فيه دروس جديدة مستقبلاً** — أي قصة جديدة تصير عبارة عن كائن (object) جديد بنفس الشكل، تضيفينه هنا وتربطينه بـ preset جديد.

### `characters.ts`
تعريف الشخصيات القابلة للاختيار:

```ts
interface Character {
  id: string;
  name: string;
  emoji: string;
  bonus: string;   // وصف الميزة الخاصة بالشخصية
  ...
}
```

فيه ٤ شخصيات: **نورة** (المستكشفة)، **فهد** (المعالج/الشافي — يعطي تلميحات مجانية بالمغامرة)، **سلمى** (المحاربة)، **عبدالله** (الحكيم). كل شخصية عندها bonus مختلف يأثر بطريقة اللعب.

### `progress.ts`
هذا الملف يدير **حفظ تقدم الطالب** بالمتصفح عن طريق `localStorage` (يعني البيانات تنحفظ بجهاز المستخدم نفسه، مو بسيرفر). فيه:

```ts
interface CompletedAdventure {
  storyId: string;
  score: number;
  maxScore: number;
  completedAt: string;
  ...
}

interface PlayerProfile {
  completedAdventures: CompletedAdventure[];
  totalXP: number;
  level: number;
  badges: string[];
  ...
}
```

- دوال لقراءة/حفظ الملف الشخصي من/إلى `localStorage`.
- منطق منح الأوسمة (badges) حسب الإنجازات.
- نظام مستويات من ٥ درجات حسب مجموع الـ XP.

> **ملاحظة صغيرة** لاحظتها أثناء المراجعة: المفتاح اللي يُحفظ فيه الملف بـ `localStorage` لسا مكتوب باسم المشروع القديم:
> ```ts
> const STORAGE_KEY = "rahhala_profile";
> ```
> هذا ما يأثر على شغل الموقع أبداً (يشتغل تمام زي ما هو)، بس لو حبيتي التناسق الكامل مع اسم "مداد" ممكن نغيّره مستقبلاً لـ `"midad_profile"` — بس لازم تنتبهين إن أي طالب جرب النسخة القديمة بنفس المتصفح بيفقد تقدمه المحفوظ (لأن المفتاح بيتغيّر). قلت لك عنها للعلم بس، مو شي مستعجل ولا يوقف شي.

---

## ٧. التنسيق والألوان (`src/index.css`)

الملف يستخدم **Tailwind CSS v4** (النسخة الجديدة اللي ما تحتاج ملف `tailwind.config.js` منفصل — الإعداد يصير مباشرة بالـ CSS وبـ `vite.config.ts`).

أهم أجزاءه:
- **استيراد خطوط جوجل**: خط **Amiri** (خط عربي كلاسيكي، يُستخدم للعناوين الكبيرة) وخط **Cairo** (خط عربي حديث، للنصوص العادية).
- **متغيرات الألوان (CSS custom properties)** بأسلوب "الواحة الصحراوية": خلفية داكنة (`--background: #090f1a`) مع لون ذهبي أساسي (`--primary: #d4a843`) يعطي إحساس دافئ صحراوي بالليل.
- **كلاسات جاهزة مخصصة** يُعاد استخدامها بكل الشاشات:
  - `.desert-gradient` — التدرج اللوني الخلفي الأساسي.
  - `.card-glass` — تأثير "الزجاج المصنفر" (glassmorphism) للبطاقات.
  - `.btn-primary` — تنسيق الأزرار الرئيسية الذهبية.
  - `.choice-btn` — تنسيق أزرار الاختيارات بالقصة.
  - `.ornament` — زخارف بصرية تراثية.
  - `.progress-bar` / `.pattern-overlay` — أشرطة التقدم والأنماط الخلفية.
- **أنيميشنز** (حركات) معرّفة بـ `@keyframes` تستخدم بأكثر من مكان (مثل `animate-float`).

---

## ٨. باغ السكرول المتكرر — تشرح ليش صار ولا يتكرر

هذا القسم مهم توثقينه لأنه نمط ممكن يتكرر لو أضفتي شاشة جديدة بنفس الطريقة الخاطئة.

**المشكلة:** أي `div` مكتوب فيه:
```tsx
className="min-h-screen ... overflow-hidden"  // أو overflow-auto
```
**وهو بنفس الوقت محشور جوا حاوية أعلى منه ثابتة الارتفاع مع `overflow-hidden`** (وهذا حال التطبيق كله، لأن `App.tsx` يحيط كل الشاشات بحاوية بارتفاع الشاشة `overflow-hidden`).

المشكلة إن `min-h-screen` تعني "ارتفاعي على الأقل = ارتفاع الشاشة، بس اقدر أكبر أكثر لو المحتوى طويل". المتصفح يحاول يكبّر الـ div لهذا المحتوى الطويل، بس الحاوية الأب فوقه مقفولة (`overflow-hidden`) فتقص أي شي يطلع عن حدودها — **قبل حتى ما يوصل الـ scroll الداخلي للـ div نفسه يشتغل**. النتيجة: محتوى مقصوص وما فيه طريقة توصلينه بالسكرول.

**الحل:** نستبدلها بـ:
```tsx
className="h-full ... overflow-y-auto"
```
`h-full` تعني "ارتفاعي بالضبط = ارتفاع أبوي، لا أكبر ولا أصغر" — يعني الـ div محصور فعلياً بمساحة معروفة. وبما إنه محصور، أمر `overflow-y-auto` يصير له معنى: أي محتوى زايد عن هالمساحة يطلع له سكرول داخلي خاص فيه، بدل ما ينقص من الحاوية الأب.

**الشاشات اللي فيها هذا الإصلاح مطبّق حالياً:**
- `SetupScreen.tsx`
- `CharacterScreen.tsx`
- `MapScreen.tsx`
- `GeneratingScreen.tsx`
- `ResultsScreen.tsx`

**لو صممتي شاشة جديدة مستقبلاً:** خذي `LandingScreen.tsx` أو `AdventureScreen.tsx` كمرجع للنمط الصحيح، وتأكدي إن الحاوية الرئيسية تستخدم `h-full` (أو `h-screen` بس بدون تعشيش جوا حاوية ثانية عندها نفس الشي) مع `overflow-y-auto`، مو `min-h-screen` مع `overflow-hidden`.

---

## ٩. ملفات الإعداد

### `vite.config.ts`
يعرّف كيف يبني/يشغّل Vite المشروع:
- المنافذ (plugins): `@vitejs/plugin-react` (دعم React) و `@tailwindcss/vite` (دعم Tailwind v4).
- اختصار `@` يشير لمجلد `src/` (عشان تقدرين تكتبين `import X from "@/components/X"` بدل مسارات طويلة بـ `../../`).
- إعداد المنفذ (port) اللي يشتغل عليه السيرفر المحلي (افتراضياً ٨٤٤٣، أو أي قيمة بمتغيّر البيئة `PORT`).

هذا الملف تم تنظيفه من كل اعتماديات Figma Make (4 إضافات كانت تحقن بيانات ديناميكية بـ `index.html` وتعتمد على ملف `.figma/make/site.json`) — الحين هو ملف Vite عادي ونظيف ما يكشف إن الموقع طلع من أداة تصميم بالذكاء الاصطناعي.

### `package.json`
معلومات المشروع: الاسم (`"midad"`)، والمكتبات المستخدمة (React 19، Vite 8، Tailwind v4، TypeScript...)، وأوامر التشغيل (`scripts`):
```json
"dev": "vite"        // تشغيل محلي للتطوير
"build": "vite build" // بناء نسخة نهائية للنشر
"preview": "vite preview" // معاينة النسخة المبنية
```

### `index.html`
نقطة الدخول. تم تبسيطه ليكون HTML ثابت عادي (عنوان الصفحة "مداد"، لغة `ar` واتجاه `rtl`، وصف ميتا بالعربي) بدل التعليقات الخاصة بـ Figma Make (`<!-- figma:title -->` وغيرها) اللي كانت تكشف مصدر الموقع.

---

## ١٠. كيف تشغّلين المشروع محلياً

من داخل مجلد المشروع، بالتيرمنال:

```bash
npm install     # مرة وحدة بس (أو كل ما تضاف مكتبة جديدة)
npm run dev     # يشغّل السيرفر المحلي، يعطيك رابط زي http://localhost:8443
```

وللبناء النهائي (لو تبين تجربين النسخة اللي بتنشر فعلياً):
```bash
npm run build
npm run preview
```

---

## ١١. خلاصة سريعة

| تبين تعدلين | روحي لـ |
|---|---|
| نص/محتوى قصة موجودة أو تضيفين قصة جديدة | `src/data/stories.ts` |
| شخصية جديدة أو تعديل ميزة شخصية | `src/data/characters.ts` |
| منطق حفظ التقدم/الأوسمة | `src/data/progress.ts` |
| شكل/ترتيب أي شاشة | الملف المطابق بـ `src/components/` |
| لون أو خط أو نمط بصري عام | `src/index.css` |
| ترتيب انتقال الشاشات ببعض | `src/App.tsx` |
| عنوان الصفحة أو الوصف بمحرك البحث | `index.html` |
