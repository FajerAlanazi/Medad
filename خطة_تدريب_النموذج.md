# خطة تدريب النموذج لمشروع مداد

ملف عملي نمشي عليه خطوة بخطوة: اختيار النموذج، تجهيز البيانات، التدريب، ودمج النتيجة بكود التطبيق. مبني على اتفاقنا: نبدأ بـ Qwen كتمرين سريع (اختياري بسبب ضيق الوقت)، وننتقل مباشرة لـ **ALLaM-7B-Instruct-preview** كنموذج المشروع الفعلي.

---

## ٠. الجدول الزمني (أقل من أسبوع)

| اليوم | المهمة |
|---|---|
| ١-٢ | تجهيز بيانات التدريب (القسم ٢ بالأسفل) |
| ٣ | اختبار سريع بعينة صغيرة (١٠-٢٠ مثال) على Kaggle |
| ٤ | التدريب الكامل + الرفع لـ Hugging Face |
| ٥ | اختبار النموذج وتحسينه لو احتاج |
| ٦-٧ | تجهيز الـ demo والعرض التقديمي |

---

## ١. اختيار النموذج — القرار النهائي

- **النموذج**: `ALLaM-AI/ALLaM-7B-Instruct-preview` (سدايا/HUMAIN)
- **الترخيص**: Apache 2.0 كامل بدون قيود إضافية (تأكدنا من ملف LICENSE نفسه) — يسمح بالتدريب والتعديل والنشر حتى تجارياً.
- **الحجم**: 7B معامل، طول سياق (context) = 4096 توكن.
- **تقنية التدريب**: LoRA / QLoRA (4-bit) — نعدّل جزء صغير من النموذج فقط، مو كل الأوزان، عشان يشتغل على GPU مجاني.
- **بيئة التدريب**: Kaggle (٣٠ ساعة GPU مجانية أسبوعياً) أو Google Colab (حصة مجانية يومية).
- **المكتبات**: `unsloth` (يسرّع التدريب ويقلل الذاكرة المطلوبة) + `transformers` + `peft` + `trl`.

> لو حبيتي تتمرنين أول على نموذج فيه نوت بوك جاهز بالكامل (`Qwen2.5-7B` من Unsloth) قبل ALLaM، هذا اختياري ومفيد لتفادي الأخطاء، لكن مو ضروري لو الوقت ضيق — تقدرين تبدين مباشرة بـ ALLaM بنفس الكود.

---

## ٢. البيانات المطلوبة

### ٢.١ الفئات (مربوطة مباشرة بأنواع العقد Node Types بكود `stories.ts`)

| الفئة | نسبة تقريبية | الوصف |
|---|---|---|
| سردي (narrative) | ٤٠٪ | فقرة قصصية تُدخل مفهوماً تعليمياً بأسلوب حكائي |
| قرار (choice) | ٢٠٪ | موقف بخيارين/أكثر، أحدها صحيح مفاهيمياً |
| سؤال (question) | ١٠٪ | سؤال مباشر متعدد الخيارات مع تفسير الإجابة |
| توصيل (match) | ١٠٪ | أزواج مفاهيم مترابطة للعبة التوصيل |
| تقييم ختامي (feedback) | ٢٠٪ | نص تحفيزي/تقييمي حسب نسبة نجاح الطالب |

**الكمية المستهدفة**: ٢٠٠-٥٠٠ مثال نظيف يكفي لهاكاثون (الجودة والاتساق أهم من العدد).

### ٢.٢ الصيغة — JSON مطابق لبنية `StoryNode`

كل مثال تدريب لازم يكون بصيغة **تعليمة → إخراج JSON** بنفس حقول الكود بالضبط، عشان تقدرين تستخدمين مخرجات النموذج مباشرة بدون تحويل يدوي:

```json
{
  "instruction": "اكتب عقدة سؤال عن مفهوم تبخر الماء، بمنطقة الأحساء",
  "output": {
    "id": "q_evaporation_1",
    "type": "question",
    "concept": "التبخر",
    "text": "لاحظت نورة أن كمية الماء بالبركة قلّت بعد يوم حار. وش السبب؟",
    "choices": [
      { "text": "تبخّر الماء بفعل حرارة الشمس", "correct": true, "nextNode": "n2", "feedback": "صحيح! الحرارة تحوّل الماء لبخار يصعد للجو." },
      { "text": "الحيوانات شربت الماء", "correct": false, "nextNode": "n2", "feedback": "احتمال وارد لكنه مو السبب الرئيسي هنا." }
    ]
  }
}
```

### ٢.٣ مصادر تسريع تجميع البيانات

- استخدمي منهج الدروس الفعلي (كتاب علوم/اجتماعيات الصف المستهدف) كمصدر للمفاهيم الصحيحة — يمنع الأخطاء العلمية بالبيانات.
- اكتبي ١٠-١٥ مثال يدوياً بنفسك أول (بأسلوبك)، وبعدها استخدمي نموذج ذكاء عام (مو مدرّب) يعمم بنفس الأسلوب على مفاهيم ثانية، وراجعيها بنفسك قبل اعتمادها — هذا أسرع من كتابة الكل يدوياً وأدق من توليد عشوائي بدون مراجعة.
- تأكدي إن كل مثال يمر عليه شخص من الفريق قبل إدخاله (خصوصاً الدقة العلمية والسلامة اللغوية).

### ٢.٤ حفظ الملف

خزّني كل الأمثلة بملف واحد `training_data.jsonl` (كل سطر = مثال JSON مستقل) — هذا الشكل القياسي اللي تتوقعه مكتبات التدريب.

---

## ٣. خطوات التدريب الفعلية (على Kaggle)

### ٣.١ تجهيز البيئة
```bash
pip install unsloth transformers trl peft accelerate bitsandbytes
```

### ٣.٢ تحميل النموذج بصيغة 4-bit
```python
from unsloth import FastLanguageModel

model, tokenizer = FastLanguageModel.from_pretrained(
    model_name="ALLaM-AI/ALLaM-7B-Instruct-preview",
    max_seq_length=4096,
    load_in_4bit=True,
)

model = FastLanguageModel.get_peft_model(
    model,
    r=16,                 # حجم تعديلات LoRA
    lora_alpha=16,
    lora_dropout=0,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],
)
```

### ٣.٣ تحميل البيانات وتنسيقها
```python
from datasets import load_dataset

dataset = load_dataset("json", data_files="training_data.jsonl", split="train")

def format_example(example):
    text = f"### التعليمة:\n{example['instruction']}\n\n### الإخراج:\n{example['output']}"
    return {"text": text}

dataset = dataset.map(format_example)
```

### ٣.٤ التدريب
```python
from trl import SFTTrainer, SFTConfig

trainer = SFTTrainer(
    model=model,
    tokenizer=tokenizer,
    train_dataset=dataset,
    dataset_text_field="text",
    args=SFTConfig(
        per_device_train_batch_size=2,
        gradient_accumulation_steps=4,
        num_train_epochs=3,
        learning_rate=2e-4,
        output_dir="outputs",
        save_steps=25,          # يحفظ نسخة كل ٢٥ خطوة (احتياط ضد الانقطاع)
        logging_steps=5,
    ),
)

trainer.train()
```

> **مهم**: راقبي رقم `loss` أثناء التدريب — إذا يقل تدريجياً فالتدريب سليم. لو ثابت أو يزيد، وقفي وراجعي صيغة البيانات.

### ٣.٥ الرفع لـ Hugging Face
```python
model.push_to_hub("اسم_حسابك/midad-allam-7b")
tokenizer.push_to_hub("اسم_حسابك/midad-allam-7b")
```

---

## ٤. دمج النتيجة بكود التطبيق

عندك مسارين حسب الوقت المتاح، ونوصي بالمسار الأول لضيق الوقت:

### المسار ١ (الأسرع والأضمن): توليد أوفلاين + لصق يدوي
1. بعد التدريب، اكتبي سكربت بسيط يعطي النموذج تعليمات (زي "اكتب ٥ عقد سردية عن دورة الماء") ويطلع لك JSON.
2. راجعي المخرجات بنفسك (دقة علمية + لغوية).
3. الصقي العقد الجاهزة داخل `src/data/stories.ts` بنفس الطريقة اللي فيها القصص الحالية (`waterCycleStory`, `foodChainsStory`) — أضيفيها كقصة جديدة أو وسّعي قصة موجودة.
4. التطبيق يبقى يشتغل بنفس طريقته الحالية (محتوى ثابت بالكود)، بس المحتوى هذي المرة "من إنتاج نموذجك المدرّب".

### المسار ٢ (لو فاضل وقت إضافي بعد الهاكاثون): ربط حي عبر API
1. تنشرين النموذج على **Hugging Face Space** (بميزة ZeroGPU المجانية).
2. تضيفين طلب (`fetch`) من كود React لهذا الـ Space وقت الحاجة لعقدة جديدة.
3. يحتاج تعديل بسيط بـ `AdventureScreen.tsx` عشان يطلب المحتوى بدل ما يقرأه مباشرة من `stories.ts`.

> بسبب ضيق الوقت الحالي، لا تحاولين المسار الثاني قبل الهاكاثون — خطر تقني عالي وغير ضروري للعرض. اعرضي النموذج كـ demo منفصل (Space بسيط) بجانب التطبيق الشغال بالمسار ١.

---

## ٥. قائمة تحقق سريعة (Checklist)

- [ ] جمع وتنظيف ٢٠٠-٥٠٠ مثال بصيغة JSONL
- [ ] مراجعة الأمثلة علمياً ولغوياً من شخص بالفريق
- [ ] اختبار الكود على ١٠-٢٠ مثال أول (تأكد إن كل شي يشتغل)
- [ ] تشغيل التدريب الكامل مع حفظ checkpoints دورياً
- [ ] رفع النموذج على Hugging Face Hub
- [ ] توليد عقد قصصية جديدة ومراجعتها
- [ ] لصقها بـ `stories.ts` واختبار التطبيق
- [ ] (اختياري) نشر demo منفصل على Hugging Face Space
